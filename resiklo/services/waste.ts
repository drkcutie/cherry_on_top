import { supabase } from '@/lib/supabase';

export const  getWaste = async () => {
    // let { data: waste, error } = await supabase
    // .from('waste')
    // .select('*')
    // return waste
    return
}